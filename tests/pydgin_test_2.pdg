@import <string>
@import <regex>
@import <iostream>
using namespace std

string inp
smatch match
regex pat ("")

int main():
	getline(cin, inp)
	if regex_match(inp, match, pat):
		cout << "Inp is of the form " << pat << endl
	else:
		cout << "Inp is not of the form " << pat << endl
	return 0
